# Engine ESP32-S3 N16R8 Compile Evidence

Status: **compile-measured against source/test commit `52055fc` on 2026-07-20;
no upload, execution, or hardware measurement was performed**.

## Tested commit

The Engine source that produced this image is anchored to commit
**`52055fc`** (`test(microworld): prove timer mixed stable compaction and tail
reuse`). That commit is test-and-comment only; it changes no timer logic, so
the complete Engine-profile firmware image is byte-for-byte identical to the
prior correction image from `98b53c6`.

## Toolchain

| Item | Recorded value |
| --- | --- |
| PlatformIO Core | 6.1.19 |
| Espressif32 platform | 7.0.1 |
| ESP-IDF | 6.0.1 (`framework-espidf @ 4.60001.0`) |
| Xtensa GCC | 15.2.0+20251204 (`toolchain-xtensa-esp-elf @ 15.2.0+20251204`) |
| Language/configuration | `gnu++17`, representative Engine consumer, exceptions and RTTI disabled |
| Environment | `esp32-s3-engine` |
| Upload/run | Not performed |

## Compile-only result

Command:

```text
C:\Users\chorn\.platformio\penv\Scripts\pio.exe run
  -d lib\microworld\tests\consumer -e esp32-s3-engine
```

| Environment | Result | Whole-image static RAM | Whole-image flash | Map |
| --- | --- | ---: | ---: | --- |
| `esp32-s3-engine` | Passed | 20,332 / 327,680 bytes (6.2%) | 208,061 / 4,194,304 bytes (5.0%) | `.pio/build/esp32-s3-engine/microworld_consumer.map` |

The 4,733,464-byte linker map passed the `Managed` profile gate: Core,
Memory, Object, and Engine archives were present; Serialization, Net, and
Integration markers were absent.

Against the original Simple Timers image (commit `338394d`, 20,332 bytes RAM
and 207,877 bytes flash), this final accepted image changed static RAM by 0
bytes and flash by +184 bytes. The flash delta was introduced entirely by the
production correction in `98b53c6` (removed destructor loop, single-pass
post-dispatch compaction, explicit mode allowlist); commit `52055fc` is
test-and-comment only and did not change the image.

These are complete ESP-IDF firmware-image figures, not isolated Engine costs.
They establish no runtime allocation delta, cycles, stack high-water mark,
thermal behavior, physical board behavior, or accepted remaining margin.

No hardware execution is needed or authorized for this compile gate. Target
runtime evidence remains blocked until explicit upload/run authorization.
